import { IconType } from "react-icons";

export default interface mainMenuItem {
    href: string
    bgColor: string
    iconColor: string
    Icon: IconType
    text: string
    content: string
  }
  